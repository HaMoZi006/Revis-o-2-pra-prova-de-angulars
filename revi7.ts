let fibo: number[] = [1, 2, 3, 4, 5, 6, 7, 9, 10]; // Array de números
let resultado: number;

// Calculando a soma dos dois últimos elementos do array
resultado = fibo[fibo.length - 1] + fibo[fibo.length - 2];

console.log(resultado); // Imprime o resultado
